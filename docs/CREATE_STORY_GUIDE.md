# 创建故事功能使用指南

## 📝 新版创建故事页面特性

### 1. **单独上传每个故事包文件**
不再批量上传，每个文件都有独立的上传按钮，方便管理和修改。

### 2. **实时格式验证**
上传后立即验证文件格式：
- ✅ **通过验证**：文件格式正确，显示绿色背景
- ⚠️ **验证警告**：文件有问题，显示黄色背景和具体错误信息
- ❌ **未上传**：必填文件显示红色背景提示

### 3. **验证规则**

#### 所有文件通用规则：
- 文件不能为空
- 至少包含一个Markdown标题（# ## ### 等）
- 最小长度50字符

#### `00_meta.md` 特殊规则：
- 必须包含YAML前置元数据（以 `---` 开头和结尾）
- 必须包含以下字段：
  - `pack_id`: 故事包ID
  - `title`: 故事标题
  - `logline`: 一句话简介
  - `era`: 时代/背景

### 4. **文件分类**

#### 必填文件（缺少无法创建）：
1. **`00_meta.md`** - 故事元信息
2. **`10_evidence_pack.md`** - 证据包 ⭐ 最重要
3. **`20_stance_pack.md`** - 立场包 ⭐ 最重要

#### 推荐文件（可选）：
4. **`30_cast.md`** - 角色卡
5. **`40_plot_outline.md`** - 剧情大纲
6. **`50_constraints.md`** - 约束与边界
7. **`60_sources.md`** - 来源清单

### 5. **操作流程**

```
1. 填写故事标题和背景描述
   ↓
2. 选择语言（中文/English）
   ↓
3. 上传必填的3个故事包文件
   - 点击"上传"按钮选择文件
   - 系统自动验证格式
   - 如有问题，显示具体错误
   ↓
4. （可选）上传推荐的故事包文件
   ↓
5. （可选）填写写作风格
   ↓
6. 点击"创建故事"提交
```

### 6. **帮助资源**

#### 在线文档（推荐）：
- 📖 [完整帮助文档](https://docs.inkpath.cc/guide/story-creator)
- 🔗 [00_meta 模板](https://docs.inkpath.cc/templates/00_meta)
- 🔗 [10_evidence_pack 模板](https://docs.inkpath.cc/templates/10_evidence_pack)
- 🔗 [20_stance_pack 模板](https://docs.inkpath.cc/templates/20_stance_pack)
- 🔗 [30_cast 模板](https://docs.inkpath.cc/templates/30_cast)
- 🔗 [40_plot_outline 模板](https://docs.inkpath.cc/templates/40_plot_outline)
- 🔗 [50_constraints 模板](https://docs.inkpath.cc/templates/50_constraints)
- 🔗 [60_sources 模板](https://docs.inkpath.cc/templates/60_sources)

#### 本地文档：
- `docs/10_故事发起者帮助文档_人类版.md`
- `docs/templates/` 目录下的所有模板文件

### 7. **常见问题**

#### Q: 为什么我的文件验证失败？
A: 检查以下几点：
- 文件是否为空或内容过短（<50字符）
- 是否包含Markdown标题（# ## ### 等）
- 如果是 `00_meta.md`，是否包含完整的YAML前置元数据

#### Q: 验证失败可以提交吗？
A: 可以，但系统会弹出确认对话框。建议修复问题后再上传。

#### Q: 可以重新上传文件吗？
A: 可以，点击"重新上传"按钮即可替换已上传的文件。

#### Q: 必须上传所有7个文件吗？
A: 不需要，只需上传3个必填文件即可创建故事。推荐文件可以提升AI续写质量。

#### Q: 文件名必须严格匹配吗？
A: 是的，文件名必须与系统要求的完全一致（如 `00_meta.md`）。

### 8. **技术细节**

#### 前端验证逻辑：
```typescript
// 验证Markdown文件格式
const validateMarkdownFile = (content: string, fileConfig: StoryPackFile) => {
  // 1. 检查是否为空
  if (!content || content.trim().length === 0) {
    return { valid: false, error: '文件内容为空' }
  }

  // 2. 对于meta文件，检查YAML前置元数据
  if (fileConfig.key === 'meta') {
    if (!content.startsWith('---')) {
      return { valid: false, error: '缺少YAML前置元数据' }
    }
    // 检查必要字段...
  }

  // 3. 检查是否有Markdown标题
  const hasHeading = content.split('\n').some(line => line.trim().startsWith('#'))
  if (!hasHeading) {
    return { valid: false, error: '文件缺少Markdown标题' }
  }

  // 4. 检查最小长度
  if (content.length < 50) {
    return { valid: false, error: '文件内容过短' }
  }

  return { valid: true }
}
```

#### 后端API接口：
```
POST /api/v1/stories
Content-Type: application/json

{
  "title": "故事标题",
  "background": "故事背景",
  "language": "zh",
  "story_package": {
    "meta": "00_meta.md的内容",
    "evidence_pack": "10_evidence_pack.md的内容",
    "stance_pack": "20_stance_pack.md的内容",
    "cast": "30_cast.md的内容（可选）",
    ...
  }
}
```

### 9. **更新日志**

#### v2.0 (2026-02-04)
- ✨ 重构为单独上传模式
- ✨ 添加实时格式验证
- ✨ 添加详细的错误提示
- ✨ 帮助文档链接指向 docs.inkpath.cc
- ✨ 改进UI/UX，更清晰的文件状态展示
- ✨ 添加上传进度提示

#### v1.0 (之前)
- 批量上传模式
- 基本的文件类型检查

---

## 🎯 核心理念

记住这句话：

> **你不是在写"设定集"，你是在准备"考古现场"。**  
> 让AI和读者去挖掘、争论、拼图，而不是直接告诉他们答案。

**关键文件**：
- 证据包决定历史感
- 立场包决定冲突
- 角色卡决定拼图

---

## 📞 需要帮助？

- 💬 在线文档：https://docs.inkpath.cc
- 📧 联系我们：support@inkpath.cc
- 🐛 报告问题：https://github.com/Grant-Huang/inkpath/issues
